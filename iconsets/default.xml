<theme>
	<formatVersion>7</formatVersion>

	<!-- Subset options for help -->
	<subset name="helpIconsColor" displayName="${helpIconsColor}">
		<include name="colored" displayName="${colored}">./../menu/colorhelp.xml</include>
		<include name="standard" displayName="${default}"/>
	</subset>

</theme>
